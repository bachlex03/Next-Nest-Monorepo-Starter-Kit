{
  "devDependencies": {
    "husky": "^9.1.7",
    "lint-staged": "^16.1.6"
  },
  "scripts": {
    "prepare": "husky"
  },
  "lint-staged": {
    "backend/src/**/*.ts": [
      "cd backend && yarn prettier:fix",
      "cd backend && yarn lint"
    ]
  }
}
